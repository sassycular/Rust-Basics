fn print_name(name: String) {
    println!("This is {}", name);
}

fn main() {
    // TODO - Assign a value to a variable
    let name = String::from("");
    // TODO - Use References to print name as many times as we do
    print_country(); // Call a function with a variable as an argument
    print_country(); // Call a function with a variable as an argument again
}
